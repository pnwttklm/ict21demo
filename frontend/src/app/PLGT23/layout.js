export default function DashboardLayout({ children }) {
    return (
        <section>
            <h1 className=' text-9xl text-white'>TEST LAYOUT</h1>
            {children}
        </section>
    )
  }